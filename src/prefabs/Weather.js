import Phaser from 'phaser';

export default class {
  constructor (game, type) {

  	createWeather(type);
  }

  createWeather(type){
    switch(option) {
    case 'Snow':
        addSnow();
        break;
    case 'Storm':
        addStorm();
        break;
    default:
        
    }
  }

  addSnow(){

  }

  addStorm(){

  }
}
