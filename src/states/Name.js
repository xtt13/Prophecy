import Phaser from 'phaser';
// import 'slick-ui' from 'SlickUI';


export default class extends Phaser.State {
	init() {
		
	}

	create() {
        // this.slickUI = this.plugins.add(Phaser.Plugin.SlickUI);
        // console.log(this.slickUI);
        // console.log(SlickUI);
	}

	update() {
    }
}
